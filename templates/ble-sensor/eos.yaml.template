project:
  name: {{PROJECT_NAME}}
  version: 0.1.0

workspace:
  backend: ninja
  build_dir: .eos/build

layers:
  - layers/core
  - layers/bsp/{{BOARD_NAME}}

toolchain:
  target: {{TOOLCHAIN}}

system:
  kind: rtos
  board: {{BOARD_NAME}}
  entry: src/main.c
  output: {{PROJECT_NAME}}.bin
