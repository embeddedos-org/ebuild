# {{PROJECT_NAME}}

Linux application for **{{BOARD_NAME}}** generated by `ebuild new`.

## Build

```bash
ebuild build

# Or directly:
gcc -o {{PROJECT_NAME}} src/main.c -I../../eos/hal/include -I../../eos/kernel/include
```

## What's included

- `src/main.c` — Linux application using EoS HAL
- `build.yaml` — ebuild build configuration
- `eos.yaml` — EoS project configuration
