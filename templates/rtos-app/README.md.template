# {{PROJECT_NAME}}

RTOS multitask application for **{{BOARD_NAME}}** generated by `ebuild new`.

## Build

```bash
ebuild build
```

## What's included

- `src/main.c` — Producer/consumer with message queue and mutex
- `build.yaml` — ebuild build configuration
- `eos.yaml` — EoS project configuration
