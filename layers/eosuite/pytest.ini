[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    gui: tests requiring a display (tkinter)
    headless: tests that run without a display
